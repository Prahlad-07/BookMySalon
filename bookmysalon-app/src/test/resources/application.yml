spring:
  datasource:
    url: jdbc:h2:mem:bookmysalon_test;MODE=MySQL;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
    username: sa
    password: ""
    driver-class-name: org.h2.Driver
  jpa:
    hibernate:
      ddl-auto: update
    open-in-view: false
  flyway:
    enabled: false

security:
  jwt:
    secret: test-jwt-secret-base64
    access-token-expiration-ms: 7200000
    refresh-token-expiration-ms: 604800000

stripe:
  api:
    key: stripe-test-key-placeholder
